# Create method `parrot` that outputs a given phrase and
# returns the phrase
def parrot(squawk)
    puts squawk
    squawk
end

output = parrot("Hello, I am a parrot")
puts "Returned phrase: " #{output}"
